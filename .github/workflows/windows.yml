name: Windows/MSYS2 make

on:
  push:
    branches: [ feature/msys2 ]

jobs:
  build:

    runs-on: windows-latest

    defaults:
      run:
        shell: msys2 {0}
    steps:
    - uses: msys2/setup-msys2@v2
      with:
        msystem: UCRT64
        update: true
        install: git mingw-w64-ucrt-x86_64-gcc mingw-w64-make mingw-w64-eigen3
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0
    - name: make
      run: make
    - name: test
      run: make test
