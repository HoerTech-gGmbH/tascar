4th order (two biquads) bandpass filter. Gain is normalized to zero at
the geometric average of the frequencies.

\begin{tscattributes}
\attr{fmin} & Minimum frequency in Hz (default: 100)   \\
\attr{fmax} & Maximum frequency in Hz (default: 20000) \\
\end{tscattributes}
